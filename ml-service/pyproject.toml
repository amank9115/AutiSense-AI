[project]
name = "manassaathi-python-ml"
version = "0.1.0"
requires-python = ">=3.10,<3.14"

[tool.uvicorn]
host = "127.0.0.1"
port = 8001

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-v --tb=short"
